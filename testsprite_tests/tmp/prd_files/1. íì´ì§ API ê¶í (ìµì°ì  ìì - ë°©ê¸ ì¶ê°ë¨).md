**1. 페이지 API 권한 (최우선 순위 - 방금 추가됨)**  
  
## /api/pages/[pageId]/route.ts가 새로 생성되었습니다. 이곳의 로직이 실제 데이터와 잘 맞는지 확인해야 합니다.  
##   
* **권한 체크 우회**:  
    * 내 페이지가 아닌 다른 사용자의 페이지 ID로   GET 요청을 보냈을 때, 정말로 **403 Forbidden**이 뜨는지, 아니면 데이터가 보이는지? (URL 파라미터 조작)  
    * GET /api/pages/[다른사람페이지ID]?userId=[내아이디] 로 테스트.  
* **존재하지 않는 페이지**:  
    * 삭제된 페이지 ID로 요청 시 **404 Not Found**가 정확히 오는지? (500 에러가 나면 서버 로직 오류임)  
**2. 초대(Invitation) 생성 로직 (신규 기능)**  
  
  
**/api/invitations/route.ts가 새로 추가되었습니다.**  
##   
* **유효성 검사**:  
    * email이나 role 없이 요청을 보냈을 때 **400 Bad Request**가 뜨는지? (DB에 쓰레기 데이터가 쌓이는 것 방지)  
* **중복/자격 검증**:  
    * 이미 초대된 사람에게 또 초대를 보낼 수 있는지?  
    * 해당 페이지의 권한(Owner/Editor)이 없는 사람이 초대를 생성하려 할 때 막히는지? (현재 코드는 주석 처리된 Mock 상태라 이 부분 구현이 안 되어 있을 수 있음 → **이게 핵심 디버깅 포인트**)  
**3. Firestore 인덱스 (기능 마비 원인)**  
테스트 리포트에서 가장 많은 실패 원인이었습니다.  
* **복합 쿼리 작동 여부**:  
    * "내 알림(Notification)"을 불러올 때 에러가 안 나는지?  
    * "초대 목록"을 필터링(예: email == 'me@test.com' AND status == 'pending') 할 때 에러가 안 나는지?  
    * *확인법*: 브라우저 콘솔에 FirebaseError: The query requires an index 링크가 또 뜨는지 확인.  
**4. Firestore 보안 규칙 (Security Rules)**  
API 엔드포인트를 통하지 않고, 클라이언트(브라우저)에서 직접 Firestore SDK로 접근할 때의 보안입니다.  
* **데이터 탈취 시도**:  
    * 로그인하지 않은 상태에서 개발자 도구 콘솔을 통해 db.collection('pages').get()을 실행했을 때 막히는지?  
    * 다른 사람의 비공개 페이지 문서 ID를 알 때, 직접 getDoc()으로 내용을 볼 수 있는지?  
**5. **Sync** API (임시 코드 확인)**  
  
  
**/api/sync/route.ts 파일이 존재하는데, 현재 Mock(가짜) 응답만 주도록 되어 있습니다.**  
**/api/sync/route.ts 파일이 존재하는데, 현재 Mock(가짜) 응답만 주도록 되어 있습니다.**  
**/api/sync/route.ts 파일이 존재하는데, 현재 Mock(가짜) 응답만 주도록 되어 있습니다.**  
##   
* 실제 동기화 기능이 필요하다면 이 부분이 구현되지 않았음을 인지하고, 클라이언트가 이 API를 호출했을 때 예상대로 동작(혹은 무시)하는지 확인해야 합니다.  
